{
    "version": 2,
    "builds": [
        {
            "src": "vercel-build.js",
            "use": "@vercel/node"
        }
    ],
    "functions": {
        "api/**/*.js": {
            "runtime": "@vercel/node"
        }
    },
    "scripts": {
        "build": "prisma generate && next build"
    }
}